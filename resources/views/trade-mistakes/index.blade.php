@extends('layouts.app')

@section('title', 'Trade Mistakes | XAUUSD Trading Journal')

@section('content')
    <div class="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-sm font-medium text-amber-300">Execution review</p><h1 class="mt-1 text-3xl font-bold text-white">Trade mistakes</h1><p class="mt-2 text-sm text-slate-400">Keep execution mistakes separate from your price action setups.</p></div><a href="{{ route('trade-mistakes.create') }}" class="rounded-lg bg-amber-400 px-4 py-2.5 text-center text-sm font-semibold text-slate-950 hover:bg-amber-300">Add mistake</a></div>
    <div class="overflow-hidden rounded-2xl border border-white/10 bg-slate-900/70"><table class="min-w-full text-left text-sm"><thead class="border-b border-white/10 bg-white/5 text-xs uppercase tracking-wider text-slate-400"><tr><th class="px-5 py-3">Mistake</th><th class="px-5 py-3">Status</th><th class="px-5 py-3">Referenced by</th><th class="px-5 py-3"><span class="sr-only">Actions</span></th></tr></thead><tbody class="divide-y divide-white/10">@forelse($mistakes as $tradeMistake)<tr class="hover:bg-white/[0.03]"><td class="px-5 py-4"><p class="font-medium text-white">{{ $tradeMistake->name }}</p><p class="mt-1 max-w-xl text-xs text-slate-500">{{ $tradeMistake->description ?: 'No description' }}</p></td><td class="px-5 py-4"><span class="badge {{ $tradeMistake->is_active ? 'badge-win' : 'badge-breakeven' }}">{{ $tradeMistake->is_active ? 'ACTIVE' : 'INACTIVE' }}</span></td><td class="px-5 py-4 text-slate-300">{{ $tradeMistake->trades_count }}</td><td class="px-5 py-4 text-right"><a href="{{ route('trade-mistakes.show', $tradeMistake) }}" class="font-medium text-amber-300 hover:text-amber-200">View</a></td></tr>@empty<tr><td colspan="4" class="px-5 py-14 text-center text-slate-400">No trade mistakes recorded.</td></tr>@endforelse</tbody></table>@if($mistakes->hasPages())<div class="border-t border-white/10 px-5 py-4">{{ $mistakes->links() }}</div>@endif</div>
@endsection
