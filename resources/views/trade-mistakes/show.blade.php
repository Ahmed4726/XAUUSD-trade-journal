@extends('layouts.app')
@section('title', $tradeMistake->name.' | XAUUSD Trading Journal')
@section('content')
    <div class="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-sm font-medium text-amber-300">Execution review</p><h1 class="mt-1 text-3xl font-bold text-white">{{ $tradeMistake->name }}</h1></div><div class="flex gap-2"><a href="{{ route('trade-mistakes.edit', $tradeMistake) }}" class="rounded-lg bg-amber-400 px-4 py-2.5 text-sm font-semibold text-slate-950 hover:bg-amber-300">Edit</a><form method="POST" action="{{ route('trade-mistakes.destroy', $tradeMistake) }}" onsubmit="return confirm('Delete this trade mistake? Referenced mistakes will be deactivated instead.')">@csrf @method('DELETE')<button class="rounded-lg border border-rose-400/40 px-4 py-2.5 text-sm font-semibold text-rose-300 hover:bg-rose-400/10">Delete</button></form></div></div>
    <section class="max-w-2xl rounded-2xl border border-white/10 bg-slate-900/70 p-5 sm:p-6"><div class="flex items-center justify-between gap-4"><span class="badge {{ $tradeMistake->is_active ? 'badge-win' : 'badge-breakeven' }}">{{ $tradeMistake->is_active ? 'ACTIVE' : 'INACTIVE' }}</span><span class="text-sm text-slate-400">Used by {{ $tradeMistake->trades_count }} {{ Str::plural('trade', $tradeMistake->trades_count) }}</span></div><p class="mt-6 whitespace-pre-line text-sm leading-6 text-slate-300">{{ $tradeMistake->description ?: 'No description recorded.' }}</p></section>
@endsection
